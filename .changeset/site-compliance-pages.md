---
'@pqc-sdk/core': patch
---

Published the FIPS 203/204/205 compliance matrices on the docs site under `/compliance/`, synced from `docs/compliance/*-MATRIX.md` by the same mechanism `scripts/sync.mjs` already uses for the Compatibility page, plus a hand-written index page and nav/sidebar entries.

Docs site only — no code, no published package output changed.
