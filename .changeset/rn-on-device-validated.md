---
'@pqc-sdk/core': patch
---

Docs only: mark React Native as validated in the compatibility matrix. The full ML-KEM-768 encrypt/decrypt and ML-DSA-65 sign/verify roundtrip ran on a physical Android device via Expo Go (SDK 54) with genuine native entropy from `react-native-get-random-values`.
