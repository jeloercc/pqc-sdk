{
  "name": "@pqc-sdk/example-cloudflare-workers",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "check": "wrangler deploy --dry-run --outdir=dist"
  },
  "dependencies": {
    "@pqc-sdk/core": "workspace:*"
  },
  "devDependencies": {
    "wrangler": "^4.111.0"
  }
}
